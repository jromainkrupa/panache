<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= t("shared.scaffold.edit.title", model: "Club") %></h1>
      <%= link_to t("cancel"), @club, class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded-xl shadow shadow-lg p-8 mt-14">
      <%= render "form", club: @club %>
    </div>
  </div>
</div>
