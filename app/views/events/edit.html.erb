<div class="container mx-auto my-8 px-4">
    <div class="max-w-3xl mx-auto">
        <div class="flex justify-between items-center mb-4">
        <h1 class="h3"><%= t("shared.scaffold.new.title", model: "Event") %></h1>
            <%= link_to t("cancel"), events_path, class: "btn btn-link" %>
        </div>

        <div class="bg-white rounded-xl shadow shadow-lg p-8 mt-14">
        <h1 class="h2 text-center mb-6">Modification d'un évenement</h1>
            <%= render "form", event: @event, action: "Modifer l'évènement" %>
        </div>
    </div>
</div>